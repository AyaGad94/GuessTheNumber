# Guess The Number

A full-stack interview assessment project built with **React**, **ASP.NET Core Web API**, and **PostgreSQL**.

The application allows users to register, log in, play a Guess the Number game, receive higher/lower hints, and keep their **best score** — the lowest number of guesses used to complete a game.

> **Interview brief:** Build a simple full-stack CRUD application using React and PostgreSQL within 2 days, store it on GitHub, and deploy it to AWS or another cloud platform.

---

## Repository

**GitHub:** https://github.com/AyaGad94/GuessTheNumber

**Live application:** https://guess-the-number-psi-one.vercel.app

**Live API:** https://guessthenumber-production-df0f.up.railway.app

---

## Project Status

### Core requirements

- [x] User registration

- [x] User login

- [x] User logout

- [x] Credentials stored in PostgreSQL

- [x] Passwords stored as secure hashes, not plain text

- [x] JWT authentication

- [x] Random target number generated from **1 to 43**

- [x] User can submit guesses

- [x] Higher/lower feedback for incorrect guesses

- [x] Current-game attempt count

- [x] Lowest number of guesses stored in one database field: `BestScore`

- [x] Returning user sees their saved best score

- [x] Better scores replace older worse scores

- [x] Worse scores never overwrite a better score

- [x] GitHub repository

- [x] Bonus feature: Guess History + Possible Range + Repeated Guess Warning

- [x] Cloud deployment

- [x] Final deployed URL added to README

---

# Application Overview

The application contains three main layers:

```text

React Frontend

      |

      | HTTP / JSON

      v

ASP.NET Core Web API

      |

      | Entity Framework Core

      v

PostgreSQL

```

For local development:

```text

React

http://localhost:5173

        |

        v

ASP.NET Core API

http://localhost:5124

        |

        v

PostgreSQL

Docker: localhost:5433

```

Production architecture:

```text
Vercel
React Frontend
https://guess-the-number-psi-one.vercel.app
      |
      | HTTPS / JSON
      v
Railway
ASP.NET Core Web API
https://guessthenumber-production-df0f.up.railway.app
      |
      | EF Core / Npgsql
      v
Neon
PostgreSQL
```

---

# Main Features

## Authentication

Users can:

- Register with username, email, and password

- Log in with email and password

- Remain authenticated while the browser session is active

- Log out by clearing the client-side authentication token

- Return later and see their saved `BestScore`

The backend uses **JWT Bearer authentication**.

Protected endpoints require:

```http

Authorization: Bearer <JWT>

```

The JWT is generated by the backend after a successful login.

The frontend stores the token in `sessionStorage`.

---

## Registration

The frontend sends:

```http

POST /api/auth/register

```

Example request:

```json

{

  "username": "PlayerOne",

  "email": "player@example.com",

  "password": "ExamplePassword123"

}

```

The backend:

1. Validates the input

2. Normalizes the email

3. Checks that the email is unique

4. Hashes the password with ASP.NET Core `PasswordHasher<TUser>`

5. Stores the user in PostgreSQL

6. Returns only safe user information

Passwords are **never stored as plain text**.

---

## Login

The frontend sends:

```http

POST /api/auth/login

```

Example request:

```json

{

  "email": "player@example.com",

  "password": "ExamplePassword123"

}

```

A successful response contains:

```json

{

  "token": "<JWT>",

  "expiresAtUtc": "<UTC timestamp>",

  "user": {

    "id": 1,

    "username": "PlayerOne",

    "email": "player@example.com",

    "bestScore": null

  }

}

```

For security, an unknown email and an incorrect password both return the same generic message:

```text

Invalid email or password.

```

This avoids exposing whether an email is registered.

---

## Restore Authentication

When the frontend refreshes, it reads the JWT from `sessionStorage` and calls:

```http

GET /api/auth/me

```

The backend validates the JWT and returns the current user.

This lets the UI restore:

- Username

- Email

- Best score

---

## Logout

The application uses stateless JWT authentication.

Logout is handled by removing the JWT from the browser:

```javascript

sessionStorage.removeItem("authToken");

```

There is intentionally no fake backend logout endpoint.

The JWT technically remains valid until its expiry time, but the client no longer sends it after logout.

---

# Guess The Number Game

## Rules

- The server generates a random integer from **1 through 43**

- The target number is never sent to the frontend

- The authenticated user submits guesses

- If the guess is too low, the API responds with `Guess higher.`

- If the guess is too high, the API responds with `Guess lower.`

- Each submitted guess counts as one attempt

- A repeated guess also counts as another attempt

- When the user guesses correctly:

  - The game ends

  - The final attempt count is calculated

  - The user's `BestScore` is updated only if the new score is better

---

## Start Game

```http

POST /api/game/start

Authorization: Bearer <JWT>

```

The backend creates the target number using:

```csharp

RandomNumberGenerator.GetInt32(1, 44)

```

The upper limit is exclusive, so this produces values from `1` through `43`.

The target number remains server-side.

---

## Submit Guess

```http

POST /api/game/guess

Authorization: Bearer <JWT>

Content-Type: application/json

```

Request:

```json

{

  "guessedNumber": 20

}

```

Example incorrect response:

```json

{

  "message": "Guess higher.",

  "attemptCount": 1,

  "isCorrect": false,

  "bestScore": null,

  "isNewBestScore": false

}

```

Example winning response:

```json

{

  "message": "Correct!",

  "attemptCount": 4,

  "isCorrect": true,

  "bestScore": 4,

  "isNewBestScore": true

}

```

---

# Best Score Design

The interview brief asks to keep track of the lowest number of guesses in the database and to keep track of one field.

The application implements that requirement with:

```text

Users.BestScore

```

`BestScore` is nullable.

A new user begins with:

```text

BestScore = NULL

```

If a user completes a game in 7 guesses:

```text

BestScore = 7

```

If the next game takes 10 guesses:

```text

BestScore remains 7

```

If a later game takes 4 guesses:

```text

BestScore becomes 4

```

The backend is the only layer allowed to update `BestScore`.

The frontend cannot submit or directly control the stored score.

---

# Concurrency-Safe Best Score Update

The backend performs a conditional database update:

```text

Update BestScore only when:

BestScore IS NULL

OR

BestScore > new attempt count

```

This prevents a worse score from overwriting a better score if multiple requests race.

The game service uses EF Core's direct update support so a full tracked `User` entity does not need to be loaded just to update one field.

---

# Data Model

## User

| Field | Type | Purpose |

|---|---|---|

| `Id` | integer | Primary key |

| `Username` | string, max 50 | Display name |

| `Email` | string, max 255 | Login identity |

| `PasswordHash` | string | Secure password hash |

| `BestScore` | nullable integer | Lowest completed-game attempt count |

The email column has a unique database index.

---

# Database

Database engine:

```text

PostgreSQL

```

Local PostgreSQL runs in Docker using:

```text

postgres:16-alpine

```

Local Docker port mapping:

```text

localhost:5433 -> container:5432

```

Database name used during local development:

```text

guess_number_db

```

The database schema is managed using **Entity Framework Core migrations**.

Current migrations include:

```text

InitialCreate

ImproveUserConstraints

```

---

# Backend Architecture

The backend uses a simple layered structure:

```text

Controllers

    |

    v

Services

    |

    +-------------------+

    |                   |

    v                   v

EF Core / PostgreSQL    GameSessionStore

```

Important backend areas:

```text

backend/GuessTheNumber.Api/

|

|-- Controllers/

|   |-- AuthController.cs

|   `-- GameController.cs

|

|-- Data/

|   `-- AppDbContext.cs

|

|-- DTOs/

|

|-- Game/

|   `-- GameRules.cs

|

|-- Models/

|   `-- User.cs

|

|-- Services/

|   |-- AuthService.cs

|   |-- JwtTokenService.cs

|   |-- GameService.cs

|   `-- GameSessionStore.cs

|

|-- Migrations/

|

|-- Program.cs

`-- appsettings.json

```

---

# Frontend Architecture

The frontend keeps UI logic and API communication separated.

```text

frontend/src/

|

|-- components/

|   |-- RegisterForm.jsx

|   |-- LoginForm.jsx

|   `-- GamePanel.jsx

|

|-- services/

|   `-- api.js

|

|-- App.jsx

|-- App.css

|-- index.css

`-- main.jsx

```

### Responsibilities

`RegisterForm.jsx`

- Collects registration data

- Calls `registerUser()`

- Displays registration status

`LoginForm.jsx`

- Collects login credentials

- Calls `loginUser()`

- Passes the successful authentication response to `App.jsx`

`GamePanel.jsx`

- Starts games

- Submits guesses

- Displays higher/lower feedback

- Displays attempt count

- Displays the current possible range

- Stores and renders Guess History for the current game

- Detects repeated guesses and shows a warning

- Reports updated `BestScore` to `App.jsx`

`services/api.js`

- Centralizes frontend-to-backend HTTP requests

- Keeps API URLs and request code out of presentation components

`App.jsx`

- Owns authenticated-user state

- Owns JWT state

- Restores authentication

- Handles logout

- Displays the current best score

- Hosts the game component

- Provides a dedicated entry screen where the user chooses Login or Create Account

---

# Game Session Design

Active games are currently stored in backend memory.

Each active game stores:

```text

User ID

Target number

Attempt count

```

The authenticated user ID is used as the key.

Important behavior:

- Starting a new game replaces any existing active game for that user

- Correctly guessing the number removes the active game

- BestScore is persistent because it is stored in PostgreSQL

- Active game state is not persistent across an API restart

For this assessment's single-instance deployment, this keeps the implementation simple and easy to explain.

For a larger distributed system, active game sessions could be moved to a shared store such as Redis.

---

# Security Decisions

The application includes the following security measures:

- Passwords are hashed using ASP.NET Core `PasswordHasher<User>`

- Plain-text passwords are never stored

- JWT signing key is not committed to Git

- Database password is not committed to Git

- Production configuration files containing secrets are ignored

- Game endpoints use `[Authorize]`

- User ID is taken from the authenticated JWT rather than from client input

- The random game target stays server-side

- The frontend cannot set `BestScore`

- Duplicate emails are protected by both application checks and a database unique constraint

- Login uses a generic invalid-credentials message

- CORS allows only configured frontend origins

---

# CORS

Local development:

```text
Frontend: http://localhost:5173
Backend:  http://localhost:5124
```

Production:

```text
Frontend: https://guess-the-number-psi-one.vercel.app
Backend:  https://guessthenumber-production-df0f.up.railway.app
```

Because the frontend and backend are different browser origins, the ASP.NET Core API uses an explicit CORS policy.

For production, Railway configures the deployed Vercel frontend as an allowed origin.

---

# Technology Versions

## Development Tools

| Technology | Version used |

|---|---:|

| .NET SDK | `9.0.317` |

| Target framework | `net9.0` |

| Node.js | `24.19.0` |

| npm | `11.17.0` |

| Git | `2.55.0.windows.3` |

| Docker | `29.7.2` |

| Docker Compose | `5.3.1` |

| PostgreSQL Docker image | `postgres:16-alpine` |

## Backend Packages

| Package | Version |

|---|---:|

| `Npgsql.EntityFrameworkCore.PostgreSQL` | `9.0.4` |

| `Microsoft.EntityFrameworkCore.Design` | `9.0.20` |

| `Microsoft.AspNetCore.Authentication.JwtBearer` | `9.0.20` |

## Frontend Runtime Dependencies

Versions declared in `frontend/package.json`:

| Package | Declared version |

|---|---:|

| `react` | `^19.2.8` |

| `react-dom` | `^19.2.8` |

During project setup, npm resolved React/React DOM to compatible `19.3.0` releases in the dependency lock.

## Frontend Development Dependencies

| Package | Declared version |

|---|---:|

| `vite` | `^8.3.0` |

| `@vitejs/plugin-react` | `^6.1.1` |

| `eslint` | `^10.10.0` |

| `@eslint/js` | `^10.0.1` |

| `eslint-plugin-react-hooks` | `^7.1.1` |

| `eslint-plugin-react-refresh` | `^0.5.6` |

| `globals` | `^17.12.0` |

| `@types/react` | `^19.2.18` |

| `@types/react-dom` | `^19.2.7` |

Observed during local builds:

```text

Vite 8.3.0

ESLint 10.10.0

```

---

# Local Development Setup

## Prerequisites

Install:

- Git

- .NET 9 SDK

- Node.js

- npm

- Docker Desktop

- pgAdmin 4 is optional but useful for viewing PostgreSQL data

## 1. Clone the Repository

```bash

git clone https://github.com/AyaGad94/GuessTheNumber.git

cd GuessTheNumber

```

## 2. Configure Local PostgreSQL

Create a local root `.env` file:

```env

POSTGRES_DB=guess_number_db

POSTGRES_USER=guessapp

POSTGRES_PASSWORD=your_local_password

```

Do not commit the real `.env` file.

Start PostgreSQL:

```bash

docker compose up -d

```

Check the container:

```bash

docker ps

```

## 3. Configure Backend Secrets

The backend requires:

```text

ConnectionStrings:DefaultConnection

Jwt:Key

```

Store them using .NET User Secrets:

```bash

cd backend/GuessTheNumber.Api

dotnet user-secrets set "ConnectionStrings:DefaultConnection" "<your-local-postgresql-connection-string>"

dotnet user-secrets set "Jwt:Key" "<your-base64-jwt-signing-key>"

```

## 4. Apply EF Core Migrations

```bash

dotnet ef database update

```

## 5. Run the Backend

```bash

dotnet run

```

Local API:

```text

http://localhost:5124

```

## 6. Configure the Frontend

Create:

```text

frontend/.env.local

```

with:

```env

VITE_API_BASE_URL=http://localhost:5124

```

Never place secrets in `VITE_*` variables because Vite exposes them to browser JavaScript.

## 7. Install Frontend Dependencies

```bash

cd frontend

npm install

```

## 8. Run the Frontend

```bash

npm run dev

```

Local frontend:

```text

http://localhost:5173

```

---

# Build and Quality Checks

## Frontend

```bash

cd frontend

npm run lint

npm run build

```

## Backend

```bash

cd backend/GuessTheNumber.Api

dotnet build

```

## Git

```bash

git diff --check

git diff --cached --check

```

---

# API Endpoints

## Public Endpoints

| Method | Endpoint | Purpose |

|---|---|---|

| `GET` | `/` | API running message |

| `POST` | `/api/auth/register` | Register a new user |

| `POST` | `/api/auth/login` | Authenticate and receive JWT |

## Protected Endpoints

| Method | Endpoint | Purpose |

|---|---|---|

| `GET` | `/api/auth/me` | Return current authenticated user |

| `POST` | `/api/game/start` | Start/restart a game |

| `POST` | `/api/game/guess` | Submit a guess |

---

# Validation

## Registration

- Username required

- Username length: 3–50

- Valid email required

- Email maximum length: 255

- Password required

- Password length: 6–128

- Email must be unique

## Login

- Valid email required

- Password required

- Password maximum length: 128

## Guess

The server validates:

```text

1 <= guessedNumber <= 43

```

---

# Performance Considerations

The backend intentionally uses efficient database-access patterns:

- Async EF Core calls

- `CancellationToken` where appropriate

- `AsNoTracking()` for read-only queries

- Projection with `Select()` when full entities are unnecessary

- Minimal database round trips

- Direct conditional `ExecuteUpdateAsync()` for BestScore

- No database query for incorrect game guesses

Typical game behavior:

```text

Wrong guess:

0 database operations

Winning guess that is not a new best:

1 lightweight projected SELECT

Winning guess that may be a new best:

1 projected SELECT

1 conditional UPDATE

Rare concurrent score race:

1 additional SELECT to return the latest score

```

---

# Known Trade-Offs

## Active game resets after backend restart

The secret target and current attempt count exist only in API memory.

```text

Backend restart:

Active game -> lost

BestScore -> preserved

```

For a multi-instance system, Redis or persistent game-session storage would be appropriate.

## JWT logout

Logout removes the JWT from the browser, but does not revoke an already-issued JWT server-side.

For stronger immediate revocation, refresh tokens and token revocation could be added.

---

# Production Deployment

The application is fully deployed using cloud services.

| Application part | Platform | Status |
|---|---|---|
| React frontend | Vercel | Deployed |
| ASP.NET Core API | Railway | Deployed |
| PostgreSQL | Neon | Deployed |
| Source code | GitHub | Published |

## Production URLs

Frontend:

```text
https://guess-the-number-psi-one.vercel.app
```

Backend:

```text
https://guessthenumber-production-df0f.up.railway.app
```

## Railway Backend Configuration

The ASP.NET Core API is deployed to **Railway using Docker**. The production container exposes the application on port `10000`.

Important Railway environment variables include:

```text
ConnectionStrings__DefaultConnection
Jwt__Key
ASPNETCORE_ENVIRONMENT=Production
PORT=10000
Cors__AllowedOrigins__0=https://guess-the-number-psi-one.vercel.app
```

Secrets are stored in Railway environment variables and are not committed to GitHub.

## Vercel Frontend Configuration

The React/Vite frontend is deployed on **Vercel** with `frontend` as the project root directory.

Production configuration:

```text
VITE_API_BASE_URL=https://guessthenumber-production-df0f.up.railway.app
```

The API base URL is public configuration, not a secret.

## Neon PostgreSQL

**Neon** hosts the production PostgreSQL database.

The production schema was created using the same EF Core migrations used locally, so the deployed database structure matches the application code.

---

# Bonus Feature: Guess History and Smart Range Tracking

The interview brief asks for one interesting bonus feature. The implemented bonus is a **session-based Guess History panel** with several UX enhancements.

Example:

```text
Guess History

6   ↑ Higher
32  ↓ Lower
20  ↑ Higher
27  ✓ Correct
```

## How It Works

`GamePanel.jsx` keeps an array of guesses in React state using `guessHistory`.

After each successful guess request, the frontend stores:

- Guessed number
- Backend feedback
- Attempt number
- Correct/incorrect status
- Repeated-guess status

The history is intentionally **session-only for the current game**. It does not require a database migration and does not replace the required persistent `BestScore` field.

## Possible Range

The frontend derives the remaining possible range from the backend's higher/lower responses.

```text
Start:       Possible range: 1 - 43
Guess 10:    Higher -> 11 - 43
Guess 35:    Lower  -> 11 - 34
Guess 20:    Higher -> 21 - 34
```

The frontend never receives the secret target number. It updates the range only from information already returned by the backend.

## Repeated Guess Warning

Before submitting a guess, the frontend checks whether the same number already exists in `guessHistory`.

If it does, the history displays:

```text
⚠ Already guessed
```

The repeated guess is still sent to the backend and still counts as an attempt. This preserves the original game rules while providing useful player feedback.

## Color-Coded Feedback

Guess History uses visual feedback to make the game easier to scan:

```text
Higher   -> purple
Lower    -> orange
Correct  -> green
Repeated -> warning style
```

## Reset Behavior

When a new game starts:

```text
Guess History -> cleared
Possible range -> reset to 1 - 43
Attempt count  -> reset
```

## Why This Bonus Was Chosen

The feature was intentionally implemented in frontend state because it:

- Adds useful player feedback
- Improves the game UX
- Requires no database schema change
- Does not interfere with `BestScore`
- Keeps the secret number server-side
- Handles the repeated-guess edge case
- Adds value without overengineering the 2-day assessment

## Additional Frontend UX

The unauthenticated UI now starts with a dedicated entry screen rather than showing Login and Register forms at the same time. The user chooses **Login** or **Create Account**, and React state renders the selected form without requiring a routing library.

The UI also includes styled Login/Create Account actions, professional Start Game/Submit Guess buttons, loading and disabled states, responsive cards, and a Personal Best display.

---

# Testing Performed

The application has been manually tested for:

- Successful registration

- Duplicate email handling

- Successful login

- Wrong password

- Unknown email

- JWT generation

- Protected endpoint without JWT

- Protected endpoint with valid JWT

- Authentication restoration after browser refresh

- Logout

- Start game

- Guess below target

- Guess above target

- Correct guess

- Attempt count

- Repeated guesses

- Repeated-guess warning

- Guess History rendering

- Possible-range updates

- Guess History reset on new game

- Input below 1

- Input above 43

- Guess without active game

- First BestScore creation

- Better BestScore update

- Worse score does not overwrite better score

- BestScore returned on future login

- Production registration through Vercel

- Production login through Vercel

- Production frontend-to-Railway API communication

- Production BestScore persistence in Neon

- Production CORS configuration

- Frontend lint

- Frontend production build

- Backend build

---

# Interview Explanation

> The React frontend handles presentation and browser state. It communicates with an ASP.NET Core Web API through JSON HTTP requests. Authentication uses JWT Bearer tokens. PostgreSQL stores user credentials as password hashes and stores one nullable `BestScore` field per user. Active games stay server-side in memory so the frontend never knows the secret number. When a game is completed, the backend conditionally updates `BestScore` only when the new attempt count is lower than the existing value. As a bonus, the frontend keeps session-only Guess History, derives the remaining possible range from higher/lower feedback, and warns about repeated guesses without changing the backend attempt-counting rules. The production frontend is deployed on Vercel, the ASP.NET Core API on Railway, and PostgreSQL on Neon.

---

# Future Improvements

- Leaderboard

- Account deletion

- Refresh tokens

- JWT revocation

- Redis-backed game sessions

- Persisted game history

- Automated backend tests

- React component tests

- CI/CD pipeline

- Rate limiting

- Structured logging

- Health-check endpoint

- Accessibility improvements

- Further responsive UI polish

---

# Author

**Aya Gad**

GitHub: https://github.com/AyaGad94

LinkedIn: https://www.linkedin.com/in/aya-gad-a55b79159/

---

## License

This project was created as a technical interview assessment.
